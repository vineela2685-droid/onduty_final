# Copy this file to .env and set real values
# Example MongoDB Atlas connection strings (replace <password> and <cluster> as needed):
# Generic example:
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority
# Example using your new account name (replace <db_password> with the real password):
# MONGODB_URI=mongodb+srv://vineela2685_db_user:<db_password>@mango.ryyyfxj.mongodb.net/onduty?appName=mango&retryWrites=true&w=majority

# Local MongoDB example:
# MONGODB_URI=mongodb://127.0.0.1:27017/onduty

# Server port (optional)
# PORT=5000
